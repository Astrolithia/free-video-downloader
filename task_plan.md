# 当前任务计划

## 目标

基于现有文档、代码和开源方案调研，产出一份“在网页中播放用户粘贴的视频链接的原视频”的可落地设计方案，并明确需要用户人工确认的决策点，再进入后续开发。

## 阶段

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 1 | completed | 恢复上下文，阅读现有规划文件、需求文档和方案设计 |
| 2 | completed | 分析前后端现有解析、流代理、下载后播放能力和限制 |
| 3 | completed | 调研可参考的开源播放器和 yt-dlp Web UI 方案 |
| 4 | in_progress | 整理项目内设计文档、更新规划记录 |
| 5 | pending | 向用户汇报方案并发起人工确认 |

## 当前决策点

1. 推荐采用“解析成功后，用户手动点击『播放原视频』按钮”而不是自动加载播放器，避免无意义的源站流量和错误暴露。
2. 推荐优先实现“平台内播放可公开访问的原视频”，不承诺登录态、会员、DRM、私密视频。
3. 推荐前端播放器升级为可扩展方案，后端继续保留统一代理层，而不是直接把源站地址暴露给浏览器。

## 已知风险

1. 许多平台并不是单个可直接播放的 MP4，而是 HLS、DASH 或音视频分离流，单纯 `<video src>` 覆盖率有限。
2. B 站等平台对 `Referer`、`User-Agent`、Range 请求较敏感，必须由后端代理和透传请求头。
3. 当前 [app/services/video_service.py](/Users/shiro/Documents/Developer/free-video-downloader/app/services/video_service.py) 的 `get_stream_source()` 只能返回单一路径，不足以完整表达“视频流 + 音频流 + manifest 类型”。
